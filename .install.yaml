- defaults:
    link:
      create: true
      relink: true

- clean: ['~']

- link:
    ~/.antigen/antigen: antigen
    ~/.antigenrc:
    ~/.gitconfig:
    ~/.zshrc:
    ~/.gpg.conf:
    ~/.hidden:

- shell:
    - [git submodule update --init --recursive, Installing submodules]
